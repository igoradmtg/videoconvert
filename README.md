# Скрипты для Обработки Видео (Video Processing Scripts) 🎬

-----

## Описание Репозитория

Этот репозиторий представляет собой обширную коллекцию утилит и скриптов, написанных преимущественно на **Python** и **Bash**, для автоматизации различных задач по обработке, конвертации, разделению, зацикливанию и микшированию видеофайлов. Инструменты созданы, чтобы упростить рутинную работу с видеоконтентом, позволяя легко:

  * **Конвертировать** форматы (например, AVI в MP4).
  * **Разделять** большие файлы на части по заданному размеру или времени.
  * **Подготавливать** видео для социальных сетей (обрезание, водяные знаки, эффекты).
  * **Зацикливать** (лупить) несколько видео и **микшировать** аудиодорожки.

Скрипты используют мощные внешние инструменты, такие как **FFmpeg** и **mkvmerge** (из набора MKVToolNix), а также популярные Python-библиотеки, включая **MoviePy** и **OpenCV**.

-----

## Установка Зависимостей 🛠️

Для работы большинства скриптов вам потребуются следующие сторонние программы и библиотеки:

1.  **FFmpeg** (должен быть установлен и доступен из командной строки для конвертации, зацикливания и микширования).
2.  **mkvmerge** (часть MKVToolNix, для точного разделения видео).
3.  **Python 3** с установленными библиотеками:
      * `moviepy`
      * `opencv-python`
      * `numpy`
      * `python-dotenv` (для работы с файлами `.env`)

Установите необходимые Python-библиотеки:

```bash
pip install moviepy opencv-python numpy python-dotenv
```

-----

## Состав и Назначение Файлов 📂

| Имя Файла | Тип | Назначение и Описание |
| :--- | :--- | :--- |
| `convert_avi_to_mp4.py` | Python | Скрипт для **конвертации** видеофайлов (включая AVI и VOB) в формат **MP4** или **MKV** с помощью **FFmpeg**. Включает проверку размера файла и повторные попытки конвертации. |
| `convert_video_to_telegram.py` | Python (MoviePy) | Инструмент для **подготовки видео для Telegram**. Выполняет обрезку, изменение размера, объединение подклипов, добавление размытия и наложение текстового **водяного знака**. |
| `fluid.py` | Python (OpenCV) | Генератор видео с простым **флюид-эффектом** или **шумом** с использованием библиотек **OpenCV** и `numpy`. |
| `lcdnova.ttf` | Шрифт (TTF) | Пользовательский файл шрифта, используемый для стилизации текстовых элементов (например, водяного знака) в скрипте `convert_video_to_telegram.py`. |
| `split_video_by_size.sh` | Bash | Простой **скрипт оболочки** для запуска скриптов разделения видео на Python под Linux. |
| `split_video_size_500m_win.py` | Python (mkvmerge) | Скрипт для **разделения видеофайлов по размеру** (**500 МБ**) с помощью **mkvmerge**. Настроен на работу в среде **Windows**. |
| `split_video_size_1500m_win.py` | Python (mkvmerge) | Скрипт для **разделения видеофайлов по размеру** (**1500 МБ**) с помощью **mkvmerge**. Настроен на работу в среде **Windows**. |
| `split_video_size_linux.py` | Python (mkvmerge) | Скрипт для **разделения видеофайлов** с использованием **mkvmerge**. Настроен на пути **Linux** и разделение по **длительности (5 минут)**. |
| `split_video_time_2_30_win.py` | Python (mkvmerge) | Скрипт для **разделения видеофайлов по длительности** (**2 минуты 30 секунд**) с помощью **mkvmerge**. Настроен на работу в среде **Windows**. |
| `video_looper_audio_mixing.env` | Конфигурация | Файл переменных окружения для `video_looper_audio_mixing.py`. Содержит настройки для входных/выходных файлов, количества **повторов видео/аудио** и **уровней громкости** микширования. |
| `video_looper_audio_mixing.py` | Python (FFmpeg) | Скрипт для **зацикливания** (конкатенации) нескольких видеофайлов и **микширования** до двух аудиодорожек (основной и дополнительной) с заданным уровнем громкости, используя **FFmpeg**. |
| `video_looper_merger_with_audio.env` | Конфигурация | Файл переменных окружения для `video_looper_merger_with_audio.py`. Содержит пути и количество **повторов видео/аудио**. |
| `video_looper_merger_with_audio.py` | Python (FFmpeg) | Скрипт для **зацикливания** (конкатенации) видеофайлов из директории и **добавления** к ним одной аудиодорожки с возможностью ее **зацикливания**, используя **FFmpeg**. |

-----

## Использование 🚀

1.  **Настройка путей:** Отредактируйте переменные директорий (`dirName`, `dirNameOut`, `INPUT_DIR` и `OUTPUT`) в начале соответствующих Python-скриптов или в файлах конфигурации `.env`.

2.  **Задание параметров:** В скриптах разделения (например, `split_video_...`) измените `fileSize` или `fileDuration`. В скриптах зацикливания — параметры `VIDEO_LOOPS` и `AUDIO_LOOPS` в файлах `.env`.

3.  **Запуск:** Выполните скрипт через интерпретатор Python:

    ```bash
    python video_looper_merger_with_audio.py
    ```

